## For shadcn setup use manual instalation:

https://ui.shadcn.com/docs/installation/manual

### The "Skip" List (shadcn)

shadcn (The Package): You do not need this as a dependency. The shadcn package is the CLI tool used to download code. It should never be in your package.json dependencies because your components should be pure React/Tailwind code by the time they hit the Library.
